import React, {useState, createContext} from "react";

export const ButtonContext = createContext();

const buttonContextProvider = () => {
    const menuButton = {


    }
    const addMoreButton = {

    }
}

export default buttonContextProvider;
